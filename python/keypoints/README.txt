Each .dat file in this folder contains a NUMPY array of
two-dimensional points extracted from the original data
set of hand images. These files were generated using the
keypoint neural network and are then used to train the
final classifier.