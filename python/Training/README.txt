This folder contains every data set image processed with the
keypoint neural network. These images are not actually used
in any part of the system and are only used as a reference.
These images can give us information about how the second 
neural network understands and expects information.